PRE_ADD = 'pre_add'  # Sent before one or more objects are added to the relation.
POST_ADD = 'post_add'  # Sent after one or more objects are added to the relation.
PRE_REMOVE = 'pre_remove'  # Sent before one or more objects are removed from the relation.
POST_REMOVE = 'post_remove'  # Sent after one or more objects are removed from the relation.
PRE_CLEAR = 'pre_clear'  # Sent before the relation is cleared.
POST_CLEAR = 'post_clear'  # Sent after the relation is cleared.
